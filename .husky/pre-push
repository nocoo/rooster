#!/usr/bin/env bash
# pre-push: G2 (Security gate) + L2 (test:e2e + gate:routes strict).
# Order: e2e tests first (must pass before route coverage is meaningful),
# then route coverage (strict), then security.
set -euo pipefail

HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=../scripts/ensure-tools.sh
source "$HOOK_DIR/../scripts/ensure-tools.sh"
require_tool osv-scanner "dependency CVE scan"
require_tool gitleaks "secrets scanning"

echo "🐓 rooster pre-push — L2 + G2"
bun run test:e2e
bun run gate:routes
bun run gate:security
